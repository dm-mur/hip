name: "hip"
version: "1.0.0"
config-version: 2

profile: "hip"

model-paths: ["models"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  hip:
    staging:
      +materialized: view
      +schema: staging

    marts:
      +materialized: view
      +schema: gold
